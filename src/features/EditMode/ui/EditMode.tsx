import styles from './EditMode.module.scss';

export const EditMode = () => {
    return (
        <button type="button" className={styles.EditMode}>
            Режим редактирования
        </button>
    );
};
